apiVersion: v1
kind: Service
metadata:
    name: "flaskdemocluster"
    labels:
        app: "flaskdemocluster"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "flaskdemocluster"
